version: 2.1
orbs: 
  matlab: mathworks/matlab@1 
jobs:
  test:
    machine:
      image: ubuntu-2004:current 
    parallelism: 2
    steps:
      - checkout  

      - run:
          name: Debug Test Files
          command: |
              echo "Glob output:"
              circleci tests glob 'tests/**/*.m'

              echo "Split output:"
              circleci tests glob 'tests/**/*.m' | circleci tests split

              echo "Awk output:"
              circleci tests glob 'tests/**/*.m' | circleci tests split | awk -F'[/.]' '{print $(NF-1) "/*"}'
workflows:
  version: 2
  try:
    jobs:
      - test
version: 2.1
orbs: 
  matlab: mathworks/matlab@1 
jobs:
  test:
    machine:
      image: ubuntu-2004:current 
    parallelism: 2
    steps:
      - checkout  
      - run:
          command: |
                    echo "Files: $(circleci tests glob 'tests/**/*.m')"
                    echo "Split: $(circleci tests glob 'tests/**/*.m' | circleci tests split)"
              
workflows:
  version: 2
  try:
    jobs:
      - test